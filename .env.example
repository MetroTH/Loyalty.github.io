# Loyalink environment variables
# Copy to .env (frontend uses VITE_-prefixed vars only)

# --- Supabase (public, safe for browser) ---
VITE_SUPABASE_URL=https://kmakycjxgxjljwjfsair.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_P4LWpWTTA3B6-63xvIjWog_XSYBYVw0

# --- Active tenant (whitelabel) ---
VITE_TENANT_SLUG=default

# --- Edge Functions / server-side only (never expose to browser) ---
# Set these as Supabase secrets, not in frontend .env
WEBHOOK_INBOUND_SECRET=change-me-hmac-secret
SERVICE_ROLE_KEY=server-side-only-service-role-key
